Updated workflow to force a clean Android build environment. It now deletes any existing android folder and regenerates it from scratch using the CLI to ensure all Gradle wrappers and permissions are correct on the CI server.
